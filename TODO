Change logging to be PDT, as the database stores things with timezones
and will thus show the viewer their current timezone.  Maybe have python
query the OS for the current timezone if that's not a PITA.
